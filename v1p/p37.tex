\documentclass{ximera}
  \outcome{Recognize that antiderivatives differ up to a locally constant function}
\begin{document}
\begin{problem}

  Suppose $f$ and $g$ are two functions with the remarkable property
  that whenever $a \approx b$ we have
  \[
  \frac{f(b) - f(a)}{b-a} \approx \frac{g(b) - g(a)}{b-a}.
  \]
  What quantity below is probably close to $g(1)$?
  \begin{multipleChoice}
    \choice{$f(1)$}
    \choice{$g(0)$}
    \choice[correct]{$f(1) + g(0) - f(0)$}
    \choice{$f(1) + f(0) - g(0)$}
  \end{multipleChoice}
\end{problem}
\end{document}
