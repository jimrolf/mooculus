\documentclass{ximera}

\begin{document}
\begin{problem}

  If $f'(a)$ exists, then $\lim_{x\to a}f(x)$
  \begin{multipleChoice}
    \choice{exists, but there is not enough information to
    determine it exactly.}
    \choice[correct]{equals $f(a)$.}
    \choice{equals $f'(a)$.}
    \choice{may not exist.}
  \end{multipleChoice}
\end{problem}
\end{document}
