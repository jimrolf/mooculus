\documentclass{ximera}

\begin{document}
\begin{problem}
If a function $f$ is not defined at $x=a$,
\begin{multipleChoice}
\choice{$\lim_{x\to a} f(x)$ cannot exist}
\choice[correct]{$\lim_{x\to a} f(x)$ could be $0$}
\choice{$\lim_{x\to a} f(x)$ must approach $\infty$}
\choice{none of the above.}
\end{multipleChoice} 
\end{problem}
\end{document}
